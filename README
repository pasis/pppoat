PPPoAT - PPP over Any Transport

PPPoAT creates point-to-point tunnel over supported transport. The transport
can be any protocol (like XMPP, HTTP, TCP, etc) or high level logic to bypass
firewalls/proxies or even a floppy net.
Every transport is implemented as a module.

Available modules:
  xmpp		PPP over XMPP protocol (PPP over Jabber)

/!\ New version is under development and doesn't work yet.
